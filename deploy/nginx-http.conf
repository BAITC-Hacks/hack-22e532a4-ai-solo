# Only this exact domain. No default_server and no changes to other sites.
server {
    listen 80;
    server_name baqbaq.world www.baqbaq.world;
    location /.well-known/acme-challenge/ { root /opt/baqbaq/acme; }
    location / { return 503 "BaqBaq: HTTPS setup pending.\n"; }
}
